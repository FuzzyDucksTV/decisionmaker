/* src/components/common/OptionCard.css */
.option-card {
    font-family: 'Poppins', sans-serif;
    border: 1px solid #f1f1f1;
    border-radius: 10px;
    padding: 16px;
    text-align: center;
    background-color: white;
    transition: transform .2s;
    cursor: pointer;
}

.option-card:hover {
    transform: scale(1.1);
}

.option-card img {
    max-width: 100%;
    height: auto;
    border-radius: 5px;
}
